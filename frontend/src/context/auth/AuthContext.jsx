import { createContext } from "react";

/* ==================================================
   AUTH CONTEXT
   Stores global authentication state and auth actions

   Provided by:
   - AuthProvider
================================================== */

export const AuthContext = createContext();

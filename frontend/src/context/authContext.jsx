import { createContext } from "react";

/* ==================================================
   AUTH CONTEXT
   Stores global authentication state and actions
================================================== */

export const AuthContext = createContext();
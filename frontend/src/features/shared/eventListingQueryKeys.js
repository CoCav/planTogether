/* ==================================================
   EVENT LISTING QUERY KEYS
   Shared URL query keys for event listing pages

   Handles:
   - view query key
   - page query key

   Notes:
   - shared by public events and current user events
================================================== */

export const EVENT_VIEW_QUERY_KEY = "view";

export const EVENT_PAGE_QUERY_KEY = "page";
